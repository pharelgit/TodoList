class Task{
  String title;
  String description;
  DateTime date;
  bool status;

  Task({
    required this.title,
    required this.description,
    required this.date,
    required this.status,
  });
}